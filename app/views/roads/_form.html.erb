<%= form_with(model: road, local: true) do |form| %>
  <% if road.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(road.errors.count, "error") %> prohibited this road from being saved:</h2>

      <ul>
        <% road.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
